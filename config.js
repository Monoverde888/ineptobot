// No quites las comillas al principio y fin de token y prefix
let config = {
    token: "NzExMzg4ODU5NDI2MTQ0MjU3.XtmeOg.Sem-APVNWau9fH3_Cpx0HXrqx-A", // Reemplaze esto con el token de tu de bot.
    prefix: "!", // El prefix de su bot para indentificar sus comandos.
  
  }
  
  // Aqui exportamos el objeto config:
  module.exports = config;
